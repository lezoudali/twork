<nav div="nav" class="nav navbar navbar-inverse navbar-fixed-top" role="navigation">
   <div class="navbar-header">
    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".top-menu">
    <span class="sr-only">Toggle navigation</span>
    <span class="icon-bar"></span>
    <span class="icon-bar"></span>
    <span class="icon-bar"></span>
    </button>
      <%= link_to "Twork", root_path, class: "navbar-brand" %>
   </div>
   <div class = "collapse navbar-collapse top-menu">

    <div class="col-sm-3 col-md-3 pull-right">

      <%= form_tag :search, class: 'navbar-form', role: 'search' do  %>
      <div class="input-group">
        <%= search_field_tag 'keyword',"", class: "form-control", placeholder: 'Search' %>
        <div class="input-group-btn">
          <%= button_tag(type: 'submit', class: "btn btn-default") do %>
            <%= content_tag(:i,"", class: "glyphicon glyphicon-search") %>
          <% end %>
        </div>
      </div>
      <% end %>


    </div>

    <% if logged_in? %>
    <p class="navbar-text">Signed in as 
       <%= link_to current_user.name, current_user %></a>
    </p>
    <p class="navbar-text navbar-right btn btn-default login">
       <%= link_to "Log Out", logout_path %></a>
    </p>
    <% else %>
      <p class="navbar-text navbar-right btn btn-default login">
        <%= link_to "Login With Twitter", "/auth/twitter" %>
      </p>
    <% end %>

   </div>
</div>
</nav>



