<!DOCTYPE html>
<html>
<head>
  <title>Twork</title>
  <%= stylesheet_link_tag 'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>
  <%= analytics_init if Rails.env.production? %>
</head>
<body>
<% unless request.env["REQUEST_PATH"] == "/" %>
  <%= render 'layouts/header' %>
<% end %>

<div class="container">
<% flash.each do |key, value| %>
  <button class="alert close" data-dismiss="alert" aria-hidden="true">×</button>
  <div class="text-center <%= flash_class(key) %>">
    <%= value %>
  </div>
<% end %>


<div class="container">
  <%= yield %>
</div>
</div>
</body>
</html>
